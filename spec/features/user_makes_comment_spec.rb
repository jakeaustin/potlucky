require 'rails_helper'

feature 'user makes comment ' do
  scenario 'on a meal' do
      # @user = create(:user)
      # @meal = create(:meal)
      # usermeal = UserMeal.create!(user: @user, meal: @meal, role: 'guest', invite_status: 'attending')

      # sign_in_as(@user)
      # visit meal_path(@meal)
      # fill_in 'Comment', with: 'So excited!'
      # click_button 'Leave Comment'

      # expect(page).to have_content 'So excited!'
  end
  scenario 'on a dish ' do
  end

end


